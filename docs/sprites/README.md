Coloque seus PNGs aqui e configure `src/sprites.js`. Hero, slime, bat e boss
aceitam PNG transparente estático ou spritesheet de quadros horizontais.
Sem configuração, ou se a imagem não carregar, a arte provisória continua ativa.
Depois execute `npm run build:pages` para incluir os arquivos no site.
