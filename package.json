{
  "name": "rpg-depthbound",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test",
    "build:pages": "npm run build && node scripts/prepare-pages.js"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "@playwright/test": "^1.55.0"
  }
}
